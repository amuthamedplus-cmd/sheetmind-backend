import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Sign Up — SheetMind | Get Started Free',
  description: 'Create your SheetMind account and start using AI inside Google Sheets. Free to start, no credit card required.',
}

export default function SignUpLayout({ children }: { children: React.ReactNode }) {
  return children
}
